# ALQQ 2.1.0 · 本地优先桌面工作区

适用于 Windows 10 / 11 x64。官网于 2026 年 9 月 11 日发布；此 GitHub Release 同步同一份已验证的安装包。

## 本次更新

- **全新品牌与登录界面**：统一桌面快捷方式、安装向导、启动加载与网站图标，更新桌面端登录工作台。
- **本地优先工作区**：支持桌面端通过本机网络执行自有 AI 和图片资源请求，图片处理与缓存可在本机完成，并提供草稿恢复和过期缓存清理。仍需登录主站并连接所用 AI / 图片服务，不是完全离线运行。
- **发布可靠性**：加固账号隔离、同账号任务互斥、异常恢复与防重复提交；对结果未确认的任务保留核查状态，避免盲目重发。
- **等待原因更清楚**：区分桌面离线、本机资源不足、同账号忙碌以及平台确认 / 审核，便于定位发布进度。
- **稳定性修复**：修复语气长度、AI 试写空正文 / 超时、登录验证、生成任务状态及取消竞态等问题；配套后端加固作品同步分页、超时和空列表判断。
- **安全更新**：加固签名内容热更新、重启握手和恢复流程，保留旧版本更新通道。

平台登录验证、风控、额度及审核规则仍需遵守；升级不代表可以绕过扫码或保证每次发布成功。

## 下载与升级

下载附件 **`ALQQ_2.1.0_x64-setup.exe`** 并运行。GitHub 下载不便时，可使用 [官网同版本下载](https://www.alqq.cn/updates/ALQQ_2.1.0_x64-setup.exe?build=96705248a514479c)。

1. 保存正在编辑的内容，等待在途发布任务结束，关闭平台后台窗口。
2. 在桌面端「设置 → 关于 → 检查更新」，或退出 ALQQ 后运行完整安装包升级。
3. 安装完成后，在「关于」中确认版本为 **2.1.0**。

**本次包含运行时与桌面启动模块变更，需要完整安装包升级，不能仅依靠旧版内容热更新。** 无需先卸载旧版；请勿手动清除用户数据目录。安装器内置 Node 与发布浏览器；桌面界面依赖 Microsoft WebView2，缺少时按安装器提示安装。

## 文件校验

- 大小：**174,796,118 字节**，约 **166.7 MiB**（约 174.8 MB）。
- 压缩：solid LZMA。
- SHA-256：`96705248a514479c364404f12bf65703a7a7e12bb9bd0c344186479ad543048b`

PowerShell 校验：

```powershell
Get-FileHash .\ALQQ_2.1.0_x64-setup.exe -Algorithm SHA256
```

附件 `.sig` 是供 ALQQ / Tauri 更新器校验的更新签名，**不是 Windows Authenticode 证书签名**。`SHA256SUMS.txt` 可用于核对下载完整性。普通用户只需下载 `.exe`。

此版本通过后端与前端测试、随包运行时检查，以及官网整包下载哈希和更新签名校验。测试不等同于替用户实际发表文章。

本仓库用于介绍、下载与反馈，不公开 ALQQ 业务源码。Linux 执行节点使用独立的 `edge-v*` 版本，与桌面端版本分开维护。
